# Retired cheap-triage research inventory — 2026-09-06

These experiments are preserved for future revival, not used by the production application. Labels are provisional machine adjudications, not independent human ground truth. No training or new labeling was performed during release preparation.

## Selected artifacts

Preserve 241 original files: 237 under `Tests/cheap_reject_evaluation/` and four investigation reports. The accompanying `cheap-triage-research-inventory.json` records every original path, byte count, SHA-256 and disposition. Six ignored Python bytecode files remain on the Windows workstation and are excluded from Git. No research was deleted.

| Experiment | Files committed | Bytes |
|---|---:|---:|
| deberta_v1 | 18 | 8,859,732 |
| initial rules/leakage | 15 | 3,384,308 |
| input_v3 | 67 | 39,390,145 |
| machine_corpus_v1 | 31 | 52,700,522 |
| supervised_v1 | 25 | 4,629,290 |
| supervised_v2 | 81 | 46,349,184 |
| top-level reports | 4 | 98,515 |

Corpora, labels, exclusion identifiers, audited examples, frozen splits, protocols, pinned model revisions, dependency locks, prediction JSON, metrics, error analyses, scripts and reports are reproducibility evidence. Saved `output/*.json` files allow analysis without rerunning inference and are intentionally retained. The six small archived `.log` files are frozen training/validation diagnostics, explicitly staged despite the general log ignore rule. They are not live service logs.

`input_v3/smoke.py` and `smoke.json` document the historical 512-token GPU memory preflight. They are experiment evidence, unrelated to the temporary application smoke deployment. No smoke deployment archives, overrides, credentials, runtime databases or build outputs are included.

Seven groups of identical files (dependency locks and the input_v3/supervised_v2 frozen inputs, baseline and rule benchmark) are retained at their original paths. Historic scripts and manifests depend on those paths. Git deduplicates identical blobs, so deleting a copy provides no object-storage benefit and would impair standalone replay.

Research paths and the four original reports have Git text conversion disabled to preserve their exact recorded bytes, including existing CRLF files. Diff and security checks remain enabled. Archive-specific whitespace attributes recognize CRLF as line endings. Exactly three frozen files (`input_v3/train.py`, `supervised_v2/train.py`, and `docs/supervised-cheap-reject-experiment.md`) retain a historical final blank line covered by their hashes, with only the EOF-blank-line warning disabled for those paths. Trailing-space and indentation checks remain enabled. New production files retain normal checks. Historic manifests remain unchanged. The refactor's `preserved-research-sha256.json` is a pre-release workstation inventory, including six excluded bytecode files; it is not a requirement that a clean clone contain generated bytecode.

## Weights retained outside Git

The three checkpoint manifests in `supervised_v1`, `supervised_v2` and `input_v3` record 66 references to 63 unique `model.safetensors` files on curiosity under `/home/codex/jsm-lab/experiments/`. These oversized model artifacts remain outside Git. `cheap-triage-checkpoint-verification.json` records successful read-only release-time existence, size and SHA-256 checks for all 63 files (22,557,527,016 bytes). Base-model revisions and inference inputs remain in the original experiment manifests. No model runtime dependency was added to JSM.

## Scope and limitations

The original reports describe the state at the time of each experiment; historical local paths and uncommitted-status statements are provenance, not current release instructions. The original first-stage README predates the later frozen eligible-cache archives; subsequent experiments carry their own self-contained snapshots and manifests. Holdout identity exclusions are preserved, not blinded holdout labels or postings for training.

The Admin maintenance prompt contains repository-relative artifact references, the user-specified configurable source checkout path, and the established SSH alias. These are operational context, not credentials. It contains no temporary smoke identity, authentication material or transient deployment data. Current release validation and hosted CI results should be read separately from the historical refactor validation report.
