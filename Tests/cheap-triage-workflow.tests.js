"use strict";
const assert=require('node:assert/strict'),api=require('../wwwroot/cheap-triage-workflow.js');
class Node{constructor(tag){this.tag=tag;this.children=[];this.handlers={};this.attrs={};}append(...x){this.children.push(...x);}replaceChildren(...x){this.children=x;}setAttribute(k,v){this.attrs[k]=v;}addEventListener(k,v){this.handlers[k]=v;}click(){return this.handlers.click?.();}}
const all=n=>[n,...n.children.flatMap(all)],text=n=>all(n).map(x=>String(x.textContent??'')).join(' '),button=(n,t)=>all(n).find(x=>x.tag==='button'&&x.textContent===t);
const review={cases:Array.from({length:29},(_,i)=>({stableJobId:String(i)})),reviewed:29,remaining:0,complete:true,counts:{KEEP:7,REJECT:22,AMBIGUOUS:0},queueVersion:'v1',queueFingerprint:'hash'};
const m={keepRecall:.995,describedKeepRecall:.99,titleOnlyKeepRecall:1,rejectionRate:.1,oldCacheRejectionRate:.13,falseRejectCount:6};const candidate={candidateVersion:'1.0.1',candidateHash:'hash',baselineVersion:'1.0.0',baselineHash:'old',humanCorrections:7,before:m,after:m,validationStatus:'PASS',warnings:['<script>data</script>'],safetyFailures:[],changedDecisions:[{id:'id',title:'Example',before:{decision:'REJECT'},after:{decision:'KEEP'}}]};
(async()=>{
let state={key:'key',revision:0,stage:'review-required',review:{...review,reviewed:0,remaining:29,complete:false}},requests=[],options,poll;
const env={setInterval:(f,ms)=>{assert.equal(ms,60000);poll=f;return 1;},clearInterval:()=>{},document:{createElement:t=>new Node(t)},HumanReview:{mount:(n,e,o)=>{options=o;}},RuleMaintenance:{copyPrompt:async p=>{env.copied=p;},open:async(b,e,o)=>{env.viewed=o.prepared;}},fetch:async(url,o)=>{
 if(o.method==='POST'){const b=JSON.parse(o.body);requests.push([url,b]);assert.equal(b.key,'key');state={...state,revision:state.revision+1};
 if(url.endsWith('/prepare'))state={...state,stage:'prompt-ready',candidate:null,disposition:null,prompt:{prompt:'compact safe prompt'}};
 if(url.endsWith('/result'))state=b.result.resultType==='NO_UPDATE_NEEDED'?{...state,stage:'no-update-needed',candidate:null,disposition:null,noUpdate:b.result.noUpdate}:{...state,stage:'candidate',candidate:b.result.candidate};
 if(url.endsWith('/decision'))state={...state,stage:b.decision,disposition:b.decision};
 if(url.endsWith('/prepare-release'))state={...state,stage:'release-request-ready',releasePrompt:{prompt:'release safely in Shadow'}};
 }return {ok:true,json:async()=>structuredClone(state)};
}};
const panel=new Node('section');const controller=api.mount(panel,env);await controller.ready;
controller.setStatus({mode:'Shadow',rulesetVersion:'1.0.1',rulesetFingerprint:'live-hash',metricsCurrent:false,live:{rejectionPercentage:8.36,totalJobs:275}});
assert.match(text(panel),/Shadow · Non-gating/);assert.match(text(panel),/8.36% · 275 cached/);
assert.equal(all(panel).find(n=>n.tag==='ol').children.length,5);
const primary=label=>{const bs=all(panel).filter(x=>x.attrs['data-workflow-primary']==='true');assert.equal(bs.length,label?1:0);if(label)assert.equal(bs[0].textContent,label);};
assert.equal(typeof poll,'function');primary('Start Human Review');assert.match(text(panel),/29 potentially unsafe/);await button(panel,'Start Human Review').click();assert.equal(options.guided,true);const draftHost=all(panel).find(n=>n.attrs['aria-label']==='Human review case');await poll();assert.ok(all(panel).includes(draftHost),'Automatic poll must preserve drafts');controller.setStatus({mode:'Off',rulesetVersion:'1.0.1'});assert.ok(all(panel).includes(draftHost),'Late status must not remount or discard a review draft');await button(panel,'Back to Workflow').click();
state={...state,stage:'review-progress',review:{...review,reviewed:17,remaining:12,complete:false}};await button(panel,'Reload Workflow').click();primary('Continue Human Review');assert.match(text(panel),/17 of 29/);
state={...state,stage:'review-complete',review};await button(panel,'Reload Workflow').click();primary('Prepare Rule Update');await button(panel,'Prepare Rule Update').click();primary('Copy Codex Prompt');await button(panel,'Copy Codex Prompt').click();assert.equal(env.copied,'compact safe prompt');assert.match(text(panel),/cannot detect when Codex finishes/);
const input=all(panel).find(x=>x.tag==='input');assert.equal(input.hidden,true);assert.equal(input.tabIndex,-1);assert.equal(input.className,undefined);let opened=0;input.handlers.click=()=>opened++;await button(panel,'Choose File').click();assert.equal(opened,1);
input.files=[{name:'large.json',size:2000001}];const count=requests.length;await input.handlers.change();assert.equal(requests.length,count);assert.match(text(panel),/below 2 MB/);
input.files=[{name:'candidate-result.json',size:500,text:async()=>JSON.stringify(candidate)}];await input.handlers.change();primary('Review Candidate');assert.equal(button(panel,'Choose File'),undefined);assert.equal(button(panel,'Copy Codex Prompt'),undefined);assert.equal(button(panel,'Approve Candidate'),undefined);
await button(panel,'Review Candidate').click();primary('Review Candidate');assert.match(text(panel),/Baseline Candidate Delta/);assert.match(text(panel),/<script>data<\/script>/);assert.match(text(panel),/Every changed decision \(1\)/);assert.match(text(panel),/Employer Title Old New Human Reason/);assert.equal(all(panel).filter(n=>n.tag==='table').length,2);assert.ok(all(panel).filter(n=>n.tag==='details').every(n=>!n.open));assert.match(button(panel,'Approve Candidate').className,/confirmation-secondary-button/);
await button(panel,'Approve Candidate').click();primary('Prepare Release Request');assert.match(text(panel),/7 reviewed false rejects corrected/);assert.match(text(panel),/22 reviewed rejects preserved/);assert.match(text(panel),/Running rules have NOT changed/);
for(const stale of ['Approve Candidate','Reject Candidate','Choose File','Import Synced Result','Copy Codex Prompt'])assert.equal(button(panel,stale),undefined,stale);
await button(panel,'View Candidate Details').click();primary('Prepare Release Request');assert.equal(button(panel,'Approve Candidate'),undefined);
await button(panel,'Change Decision').click();await button(panel,'Reject Candidate').click();primary('Prepare Revision Request');assert.equal(button(panel,'Approve Candidate'),undefined);
await button(panel,'Change Decision').click();await button(panel,'Approve Candidate').click();await button(panel,'Prepare Release Request').click();primary('Copy Release Prompt');await button(panel,'Copy Release Prompt').click();assert.equal(env.copied,'release safely in Shadow');assert.equal(button(panel,'Prepare Release Request'),undefined);assert.equal(button(panel,'Approve Candidate'),undefined);
state={...state,stage:'released',release:{rulesetVersion:'1.0.1',candidateHash:'hash',deploymentIdentity:'commit',validationStatus:'PASS'}};await button(panel,'Reload Workflow').click();primary(null);assert.match(text(panel),/Release Complete/);assert.equal(button(panel,'Change Decision'),undefined);assert.ok(!all(panel).some(n=>n.attrs['aria-label']==='Human review case'));await button(panel,'Revisit Human Review').click();assert.ok(all(panel).some(n=>n.attrs['aria-label']==='Human review case'));await button(panel,'Back to Workflow').click();primary(null);
state={...state,stage:'prompt-ready',candidate:null,disposition:null,prompt:{prompt:'safe result request'}};await button(panel,'Reload Workflow').click();
const noUpdate={rulesetVersion:'1.0.1',metrics:m,humanMatches:Array.from({length:29},(_,i)=>({id:String(i),decision:i<7?'KEEP':'REJECT'})),changedDecisionCount:0,validationStatus:'PASS',evaluationArtifactHash:'artifact'};
const resultInput=all(panel).find(n=>n.tag==='input');resultInput.files=[{name:'candidate-result.json',size:1000,text:async()=>JSON.stringify({resultType:'NO_UPDATE_NEEDED',noUpdate})}];await resultInput.handlers.change();primary(null);
assert.match(text(panel),/Cheap Triage Maintenance Complete/);assert.match(text(panel),/No Rule Update Needed/);assert.match(text(panel),/Ruleset 1.0.1 is current/);assert.match(text(panel),/The previous maintenance cycle is complete. No additional rule update is required/);assert.match(text(panel),/29 \/ 29 complete/);assert.match(text(panel),/KEEP 7\s+REJECT 22\s+AMBIGUOUS 0/);
assert.ok(all(panel).find(n=>n.tag==='ol').children.every(n=>!n.attrs['aria-current']));assert.match(text(panel),/not required/);
for(const label of ['Choose File','Import Synced Result','Approve Candidate','Reject Candidate','Prepare Release Request','Copy Release Prompt','Copy Codex Prompt','View Prompt','Prepare Rule Update','Start New Maintenance Cycle'])assert.equal(button(panel,label),undefined,label);
assert.doesNotMatch(text(panel),/Codex Analysis Required|Not prepared|Not evaluated|upcoming/);assert.match(all(panel).find(n=>n.attrs['aria-label']==='Current workflow action').className,/admin-evaluation-card/);assert.ok(!all(panel).some(n=>n.tag==='input'));await button(panel,'Reload Workflow').click();primary(null);
for(const n of all(panel).filter(x=>x.tag==='details'))assert.ok(!n.open,'Diagnostics collapsed by default');for(const n of all(panel).filter(x=>x.tag==='button'))assert.match(n.className,/primary-button/);
const fs=require('node:fs'),path=require('node:path');const css=fs.readFileSync(path.join(__dirname,'../wwwroot/styles.css'),'utf8').split('/* Cheap Triage desktop workflow:')[1];assert.match(css,/94rem/);assert.match(css,/\.triage-current-action \{[^}]*padding: 1rem;[^}]*background: var\(--color-surface-secondary\)/);assert.match(css,/\.triage-workflow-status:empty \{ display: none; \}/);assert.match(css,/repeat\(5, minmax\(0, 1fr\)\)/);assert.match(css,/@media \(max-width: 700px\)/);assert.match(css,/overflow-x: auto/);assert.doesNotMatch(css,/position:\s*(absolute|fixed)|#[0-9a-f]{3,8}\b/i);
state={...state,detection:{status:'idle',newObservations:37,message:'37 new Shadow observations collected; no review required yet.'}};await poll();primary(null);assert.match(text(panel),/No action required/);assert.match(text(panel),/37 new Shadow observations collected/);
state={...state,stage:'review-required',noUpdate:null,review:{...review,queueVersion:'auto-v1-test',cases:review.cases.slice(0,12),reviewed:0,remaining:12,complete:false,counts:{KEEP:0,REJECT:0,AMBIGUOUS:0}}};await poll();primary('Start Human Review');assert.match(text(panel),/12 new Cheap Triage decisions need review/);assert.doesNotMatch(text(panel),/Not prepared|No candidate baseline/);
const detector=fs.readFileSync(path.join(__dirname,'../CheapTriageMaintenanceDetector.cs'),'utf8');assert.doesNotMatch(detector,/HttpClient|JobSourceClient|WorkspaceRuntimeProvider|ReconcileCheapTriageAsync|SaveJobsCacheAsync/);
assert.ok(requests.every(([url])=>!url.includes('/deploy')));assert.equal(Object.keys(api.stages).length,10);
console.log('PASS all ten single-next-action states, import hiding, disposition, revision/release handoff, safe themed rendering and no deployment');
})().catch(e=>{console.error(e);process.exitCode=1;});
